apiVersion: apps/v1
kind: Deployment
metadata:
  name: media-service
  namespace: webrly
  labels:
    app: media-service
    service: media
spec:
  replicas: 3
  selector:
    matchLabels:
      app: media-service
  template:
    metadata:
      labels:
        app: media-service
        service: media
    spec:
      containers:
      - name: media-service
        image: node:20-alpine
        command:
        - sh
        - -c
        - |
          cat > server.js << 'EOF'
          const http = require('http');
          const server = http.createServer((req, res) => {
            res.writeHead(200, { 'Content-Type': 'application/json' });
            res.end(JSON.stringify({
              service: 'media-service',
              status: 'healthy',
              timestamp: new Date().toISOString(),
              version: '1.0.0'
            }));
          });
          server.listen(3006, () => {
            console.log('Media Service running on port 3006');
          });
          EOF
          node server.js
        ports:
        - containerPort: 3006
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: webrly-config
              key: NODE_ENV
        - name: DATABASE_URL
          value: "postgresql://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@postgres-media-service:5432/webrly_media"
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: webrly-secrets
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: webrly-secrets
              key: POSTGRES_PASSWORD
        - name: REDIS_URL
          value: "redis://redis-service:6379"
        - name: AUTH_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: microservices-config
              key: AUTH_SERVICE_URL
        - name: UPLOADTHING_SECRET
          valueFrom:
            secretKeyRef:
              name: webrly-secrets
              key: UPLOADTHING_SECRET
        - name: UPLOADTHING_APP_ID
          valueFrom:
            secretKeyRef:
              name: webrly-secrets
              key: UPLOADTHING_APP_ID
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /
            port: 3006
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: 3006
          initialDelaySeconds: 30
          periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: media-service
  namespace: webrly
  labels:
    app: media-service
spec:
  selector:
    app: media-service
  ports:
  - port: 3006
    targetPort: 3006
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: media-service-hpa
  namespace: webrly
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: media-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80 