apiVersion: apps/v1
kind: Deployment
metadata:
  name: crm-service
  namespace: webrly
  labels:
    app: crm-service
    service: crm
spec:
  replicas: 3
  selector:
    matchLabels:
      app: crm-service
  template:
    metadata:
      labels:
        app: crm-service
        service: crm
    spec:
      containers:
      - name: crm-service
        image: node:20-alpine
        command:
        - sh
        - -c
        - |
          cat > server.js << 'EOF'
          const http = require('http');
          const server = http.createServer((req, res) => {
            res.writeHead(200, { 'Content-Type': 'application/json' });
            res.end(JSON.stringify({
              service: 'crm-service',
              status: 'healthy',
              timestamp: new Date().toISOString(),
              version: '1.0.0'
            }));
          });
          server.listen(3003, () => {
            console.log('CRM Service running on port 3003');
          });
          EOF
          node server.js
        ports:
        - containerPort: 3003
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: webrly-config
              key: NODE_ENV
        - name: DATABASE_URL
          value: "postgresql://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@postgres-crm-service:5432/webrly_crm"
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: webrly-secrets
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: webrly-secrets
              key: POSTGRES_PASSWORD
        - name: REDIS_URL
          value: "redis://redis-service:6379"
        - name: AUTH_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: microservices-config
              key: AUTH_SERVICE_URL
        - name: AGENCY_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: microservices-config
              key: AGENCY_SERVICE_URL
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /
            port: 3003
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: 3003
          initialDelaySeconds: 30
          periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: crm-service
  namespace: webrly
  labels:
    app: crm-service
spec:
  selector:
    app: crm-service
  ports:
  - port: 3003
    targetPort: 3003
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: crm-service-hpa
  namespace: webrly
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: crm-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80 